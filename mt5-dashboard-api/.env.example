# FastAPI Application Settings
# Copy this file to .env and update values

# App Settings
APP_NAME="MT5 Dashboard API"
VERSION="1.0.0"
DEBUG=false

# Server Settings (for VPS deployment)
HOST=0.0.0.0
PORT=8000
RELOAD=false

# Performance Settings (optimized for VPS)
CACHE_TTL=300
CACHE_MAX_SIZE=100
MAX_WORKERS=2
ENABLE_GZIP=true

# Paths
CONFIG_PATH=config/settings.yaml
DATA_PATH=data
LOGS_PATH=logs
